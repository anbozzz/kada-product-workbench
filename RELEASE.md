# 安装包发布

本文件供维护者使用；用户与安装 Agent 从 [INSTALL.md](INSTALL.md) 开始。官方仓库为 [anbozzz/kada-product-workbench](https://github.com/anbozzz/kada-product-workbench)，归档与 SHA256SUMS 一起发布到 GitHub Releases；未发布到 npm。

## 0.12.7 发布闭环修复

- 修复宽度小于 1280px 时绑定模式、评审模式缺少可访问名称的问题；保留图标、模式切换及原画布，增加窄屏键盘切换回归。
- 源码根目录直接跟踪官方仓库 main。README 为产品首页，DEVELOPMENT 为工程说明；保留远端作者修改。旧本地历史保存在本机 safety/local-before-github-sync，不对外推送该备份分支。
- `.github/workflows/verify.yml` 对 main 推送和 PR 执行统一验证与安装包构建，失败保留浏览器诊断。
- 普通推送只验证。明确发布时，先提升 package.json/package-lock.json 版本，使用以 `release:` 开头的提交消息推送 main，或在 main 手动运行工作流并选择 release；验证成功后才生成该版本 Tag、Release、安装归档和 SHA256SUMS。已有 Release 拒绝覆盖。
- 日常更新先 fetch 并核对远端作者修改，再在当前开发目录提交和推送；不再使用 output 下的副本作为开发或发布源，不覆盖旧版本附件。
- Windows/Linux 创作端、第二台设备及新任务中的 Hook 信任仍需对应实机验收。

## 0.12.6 共享 Skill 随包交付（候选）

- PRD、Spec、独立架构与表达整理方法纳入源码发行快照；客户不再需要维护者的 Workspace 或个人 Skill。
- 默认 `package:plugin` / `package:release` 无需 `--skills-root`；保留显式输入兼容，包内仍包含七项 Skill、相对 CLI 入口、运行依赖和文件校验清单。
- 共享方法更新采用显式同步与只读比较，随新版本升级；不会静默更新客户规则或业务文档。
- 验证：三项共享方法的 21 个文件逐一一致；Skill 格式、引用、快照漂移、默认归档解压、显式输入兼容和插件结构检查通过。`npm run verify` 通过，含 204 项 Node 测试和全部真实浏览器旅程。
- macOS 隔离真实 CLI 完成移动目录后的预检、安装、重复安装、安装副本工作台及 MCP/SessionStart 检查，目标项目文件未改写；没有修改正常用户安装。
- 候选包：`output/releases/0.12.6-candidate/interactive-product-spec-0.12.6.tar.gz`，同目录附 `SHA256SUMS`。包基于当前工作区，包含本轮之前已有的工作台改动；源码尚未统一提交。未公开上传，Windows/Linux 实机及客户新任务中的信任与加载仍待验证。

## 0.12.4 空闲等待修复（2026-09-06）

- 将 PRD 评审的 50 秒模型续询改为插件内事件等待，默认最长 12 小时；客户端超时配置为 43260 秒。短探测、等待到期和连接失败不再要求模型自动续询，也不建立终端轮询替代链。
- 支持按 MCP 请求取消等待、连接关闭清理、会话单等待保护；批注队列、停止、两轮发布、确认和原工作台入口保留。无新增模型任务或正文写入途径。
- 统一 `npm run verify` 通过：170 项 Node 测试及 11 条浏览器旅程；新增跨 51 秒空闲边界不返回结果、提交唤醒、取消不领取后续批注及 MCP 取消测试。末次取消检查与连接状态调整另经定向回归通过。
- 隔离 Codex 安装确认工具超时配置实际解析为 43260 秒；已安装连接需重新加载。程序挂起与浏览器回归不等于真实账户零消耗证明，宿主调度和后续实际修订仍会使用额度。
- 实际安装：官方 `codex plugin add` 已安装并启用 0.12.4，1,424 个文件与最终包逐一一致；正常配置解析的 `tool_timeout_sec` 为 43260。0.12.3 来源备份保存在 `<维护者本地目录>`，未手改缓存或 Hook 信任。
- 最终包：`output/releases/0.12.4-final/`。`output/releases/0.12.4/` 为本次安装验证前的候选包，不用于正常安装；旧版本归档保留，未公开上传。

## 准备与打包

1. 确认 `package.json` 的版本代表本次唯一发布内容；同版本不得替换文件。
2. 仓库 `plugin-skills/` 已包含七项 Skill。三项共享方法的维护源仍在维护者的共享目录；更新后先执行 `npm run sync:skills -- --source "共享 Skill 根目录"`，评审并提交三项完整快照及 `shared-snapshot.json`。用同一命令加 `--check` 可只检查是否同步。来源路径必须显式传入，不保存开发机绝对路径、不从安装缓存取规则。客户从独立源码 checkout 构建时无需共享目录；默认打包检查仓库快照与引用完整性。
3. 按 lockfile 安装根目录和 `ui/` 的开发依赖，执行 `npm run verify`。源码构建还需满足 `ui` 构建工具的 Node 引擎要求；与安装包最低运行版本区分。
4. 在已构建的源码根目录执行：

```bash
npm run package:release -- --output "新的发布输出目录"
```

生成一个自包含 marketplace 目录、`interactive-product-spec-版本.tar.gz` 和 `SHA256SUMS`。需要系统 `tar`；输出目录必须不存在，避免覆盖。包内包含预构建 UI、生产依赖、七项 Skill、安装器和文件校验清单；不需要目标用户构建工作台源码。不携带源码仓库中的项目、会话、账号配置或本机路径。

新增四项能力由本仓库 `plugin-skills/` 提供：grilling、diagram-design、ui-ux-pro-max、ant-design-html。第三方原说明、模板、脚本、数据和许可证随包保留，中文入口及 HTML 接续由本项目维护，来源记录见 `plugin-skills/sources.json`。不要用安装者的个人 Skill 覆盖这些发布输入；目标页面的 Ant 依赖由 Agent 在对应 Project 安装，不增加工作台 UI 依赖。

维护者兼容重建可显式传入 `--skills-root "三项共享 Skill 根目录"`；此时校验并记录实际输入，日常发布使用仓库快照。

只需要目录包时使用 `npm run package:plugin -- --output "空目录"`。安装方法只有一份 INSTALL.md，builder 原样带出，不内嵌打包机绝对路径。

## 发布前门禁

- 将归档移到包含中文和空格的新目录，校验并解压，执行 `node install.mjs --check`。
- 用隔离 Codex 用户配置执行 `node scripts/verify-plugin-install.mjs "解压包根目录"`；该脚本不修改正常用户安装，测试真实 CLI 安装、重复安装、实际缓存中的工作台入口和目标项目扫描。
- 按 [INSTALL.md](INSTALL.md) 在新的真实用户任务中验证七项 Skill、MCP 和用户 Hook 信任。Windows 需 Windows 实机验证，不能以 macOS 或假命令测试代替。
- 审查全部 Skill 与依赖的再分发授权、包内容及第三方许可证；MIT 项目声明不能替代这些检查。
- 从移动后的安装包调用 Ant 起步脚本，在空目录生成 PC 与 APP 工程、安装固定依赖、构建并用浏览器打开最终 HTML；检查主操作及原工作台加载。示例运行和干净环境安装不代表任意用户 Agent 的生成质量已验收。
- 只有上述对应环境已验证，才在发布说明列为支持；尚未验证项明确列出。

## 公开发布时

确定仓库后，将 `.tar.gz` 与 `SHA256SUMS` 一起附加到同一版本 Release，并在仓库 README 提供 INSTALL.md 的链接。安装说明会从用户确认的同一仓库定位发布包，不需要写入任何开发机路径。源码仓库应一并提交 `plugin-skills/` 的完整快照及清单，使接收者可重建同版本规则。

本地打包、测试通过不等于发布完成。本流程不自动创建仓库、上传 Release、信任脚本或改变仓库可见性。

## 当前验证边界（0.12.3，2026-09-06）

- 新增 PRD 首次生成后的评审时机选择：开始后才打开原工作台；稍后或关闭保留文档与原任务继续入口。已有明确授权与批注修订不重复询问，同版本选择可跨工具重连保留。
- 显式 PRD 批注支持没有 HTML：同一阅读器全宽展示，关闭后可重新打开；有 HTML 时原画布与批注交互保持。
- 统一 `npm run verify` 通过（165 项 Node 测试及 11 条浏览器脚本），另补充并通过开始后工具重连恢复原会话测试。源码 PRD 格式与差异检查通过。
- 发布包保存于 `output/releases/0.12.3/`，未公开上传。通过官方 `codex plugin add` 升级本机已有 `interactive-product-spec@personal`；安装副本 1,423 个文件与清单一致，安装副本无 HTML 评审入口和服务配置复验通过。
- 原个人安装来源保留于 `<维护者本地目录>`。未修改其他插件来源、缓存内容或 Hook 信任。当前任务已加载的工具不热替换，新任务才加载新版；客户端原生表单外观和真实 Agent 生成后的全流程仍需在新任务体验验证，不把模拟回答和模型修订当作人工验收。

## 历史验证边界（0.12.2，2026-09-06）

- 仅重构既有产品文档、架构与统一 Spec 三项 Skill 的协作：PRD 生成完整产品定义，经过本批评审后，Spec 按功能融合产品决定和必要工程机制。复杂分支在生成阶段设计，精简不省略条件和恢复。局部修改、HTML 同轮同步、真实确认及授权边界保留；未新增 Skill 或工作台功能。
- 独立试用：在临时目录联合生成三类搜索、批量归档与通知、本地展开开关的 PRD 和 Spec 草稿；另一次试用仅改指定按钮文字，其余正文逐字保留。首次试写暴露模块标题与节点元数据格式问题，修正原标准及临时样本后，实际读取器解析 14 个节点、7 个页面，错误和警告均为 0。生成内容含具体状态、分支与机制；普通提案、外部合同缺口与产品确认分别标明。共用节点其他页面仍为正文引用，本次未更改映射模型。
- 运行基线：隔离副本取自 `e70d4c321e3cf65a21175de45162e520d75f090a`，排除工作区原有未提交的 `src/prd-review-mcp.mjs` 修改。该副本的 `npm run verify` 通过，含 153 项 Node 测试、构建/契约及 10 条浏览器脚本；之后仅更新方法文档与版本元数据。未将原有工作台功能归为本次修改。
- 方法与安装：三项 Skill 结构、34 个本地引用、PRD 解析通过；最终包在中文空格临时路径完成预检、真实 CLI 安装、重复安装、安装副本启动、MCP 与目标文件未改写检查。归档校验通过。通过官方 `codex plugin add` 安装 0.12.2；1,422 个安装文件符合清单，三项 Skill 的 25 个源文件与共享目录一致。官方 `skills/list` 强制重扫返回八项已启用的 0.12.2 随包 Skill，未返回该插件旧版路径。
- 发布包在 `output/releases/0.12.2/`，0.12.1 为本轮格式复验前的本地过渡包；均未公开上传，不覆盖旧归档。原安装来源保存在 `<维护者本地目录>`；未手改缓存或 Hook 信任。
- 验证边界：独立试写和结构检查不保证未来任意需求一次生成正确，也不等于真实软件实施验收。正常新任务中的完整 Agent 执行、Windows、最低 Node 版本与既有 Ant 模板没有新增实机支持声明。MDT 的现有 PRD、Spec 与页面未在本轮修订。

## 0.12.0 验证记录（2026-09-05）

- 随包能力：原四项文档 Skill 保留；新增 grilling、diagram-design、ui-ux-pro-max 与 ant-design-html 的中文入口、所需模板、脚本、数据和第三方许可证。新项目可分别使用 Ant Design／Ant Design Mobile 生成 PC／APP 形态 HTML，由用户当前 Agent 执行，目标 Project 不需要已有个人 Skill。既有工程按原组件体系接续。
- 验证：`npm run verify` 通过，包含 152 项 Node 测试、架构／契约／构建检查及现有 9 条工作台浏览器脚本。新起步脚本从含中文空格的移动目录运行，拒绝覆盖已有工程；缺少任一要求的 Skill 资源时就绪检查失败。四项新 Skill 入口结构校验通过。
- 安装后实测：迁移后的安装包在隔离配置完成预检、安装、重复安装、工作台启动、目标文件不变、MCP 与 SessionStart 检查。实际安装副本的 UI 查询及流程图校验脚本可运行；从其中的起步脚本在两个空目录创建 PC／APP 工程、安装依赖并构建单文件 HTML。浏览器验证离线打开、输入校验、新增与完成状态、手机列表／详情／返回、长中文和窄屏，随后从原工作台加载两端页面并继续打开关联 PRD；截图见 `output/playwright/ant-design-html/`。
- 发布与当前安装：包位于 `output/releases/0.12.0/`，发布清单共 1,425 个文件。通过官方 `codex plugin add` 更新本机 `interactive-product-spec@personal` 至 0.12.0，已启用，1,422 个插件安装文件与清单一致。原来源保留在 `<维护者本地目录>`；未手工修改缓存或 Hook 信任。发布包来自本轮验证的当前工作区，保留此前已有源码与文档修改，未宣称它们属于本轮提交；未公开上传。
- 文档与验收边界：已核对现有 PRD／Spec，本轮新增的是随包方法和生成基础，工作台页面行为及文档职责无需修订；能力概览、使用方式和打包责任分别写入 README、Skill、安装架构。测试 HTML 是临时夹具，不是用户业务交付物。新任务的自动选用、真实用户需求的生成效果、Windows 和最低 Node 版本实机仍未验收；首次生成安装依赖需要网络或缓存，不以模板可运行承诺任意需求均已实现。

### 三轮反向复核

1. 目标：执行者始终是用户 Agent，成果是 PC／APP 形态 HTML；审问、图形、设计与实现按当前任务接续，没有新增工作台模式，也没有改为原生客户端。
2. 独立使用：安装副本包含八项 Skill 及必要资源，脚本按自身位置解析资源；空项目无需个人 Skill 或竞品。检查并约束 UI 查询可能建议下载宣传页的偏差，业务结构仍服从 PRD／Spec。
3. 兼容与证据：已有工程不强制迁移组件体系，起步脚本拒绝覆盖；原工作台旅程、两端页面与安装验证通过。示例效果、方法结构和新任务的 Agent 实际执行质量分别记录。

## 0.11.5 验证记录（2026-09-04）

- 新能力：在原 HTML 完成协议 MCP 中加入 PRD 批注评审工具，支持同一工作台内就地评价、反馈队列、处理中停止、修订发布及正式确认状态；工作台仍保持页面、PRD、Markdown Spec、内部投影和人工 Map 的来源边界。
- 验证：`npm run verify` 通过，包含架构与契约检查、143 项 Node 测试及 6 类真实 Chrome 工作台旅程；首次受限运行只在 Chrome 进程权限处失败，获准后完整通过。构建仍有既有 CSS `::highlight` 解析提示和大 chunk 提示，不影响门禁结果。
- 发布与安装：从提交 `39af95b` 的已验证源码补发唯一版本 0.11.5，经自包含发布包打包与清单校验后，通过 Codex 官方 `plugin add` 将本机 `interactive-product-spec@personal` 更新为 0.11.5；安装缓存与发布包逐文件一致。原 0.11.4 来源保留在 `<维护者本地目录>`，未修改 Hook 信任。
- MDT 实测边界：当前 Markdown Spec 含 444 个业务节点，旧项目投影仅含 9 个节点。工作台创建的可见 Codex 投影任务完整解析后因单次响应容量不足而拒绝删减结果；改用等价本地解析生成的临时投影后，39 个页面、444 个节点通过 v0.1 契约并成功启动映射工作台。高置信候选在首页患者中心入口和会诊视频卡主动作达到 96%，但患者中心“打开患者详情”仍错误命中页标题，跨页动作也会被当前页 32% 低质量候选阻止自动导航，因此不能宣称 MDT 全量匹配质量已经闭环。
- 接入边界：当前既有任务不会热加载升级后的 Skill 与 MCP；新任务仍需检查四项 Skill、合并后的 HTML/PRD MCP 及 Hook 信任。临时投影和临时空 Map 不写入 MDT Project，候选定位不等于人工映射确认。

## 0.11.4 验证记录（2026-09-03）

- 方法修订：产品文档 Skill 维护 PRD 先行、本批 Spec 与已授权实现的衔接；Spec 编写、工作台数据转换和界面操作分别按需读取；架构必要性、产品工程约束和独立文件规则分开。当前 Codex 任务负责本地文档同步，生成或同步不代表用户确认。工作台界面、节点写回与 Hook 协议未改动。
- 必要读取路径：普通 PRD 从 6,821 变为 5,207 字符；不改变架构的局部 Spec 修订从 10,634 变为 6,066；持续 HTML 产品从 PRD 到交付后同步的累计方法体量从 24,449 变为 15,723。每份文件在对应路径中计一次，不含项目输入资料，不是 token、单步读取或峰值上下文。原路径按阶段新增为 PRD 6,821、Spec 10,634、架构 6,994；新路径按阶段新增为 PRD 5,207、Spec 6,066、架构 3,451、HTML 文档同步 999。
- 内容复核：按上述三类任务对照阶段、范围、事实来源和写入边界；页面唯一性、路由、模块职责、组件复用、状态数据分离、源码构建分离、交付打开方式及验收要求均保留。修正“删除不适用小节”可能误删固定 PRD 小节的表述，并明确无页面证据的 pageId 使用省略或 null。
- 验证：三项 Skill 结构及本地引用检查通过；标准中的 PRD/Spec 示例通过解析，局部节点修改保留稳定 ID、行为、工程与验收章节。13 项相关定向检查及项目统一 `npm run verify` 通过，统一验证含 130 项 Node 测试和 6 条真实浏览器流程。测试截图另存后恢复原有文件，已有源码修改保留。
- 发布与安装：归档哈希与条目检查、中文空格路径解压、隔离预检/安装/重复安装、安装副本启动、资源、目标文件不变、MCP 和 SessionStart 检查通过。通过 Codex 官方命令更新本机 `interactive-product-spec@personal` 至 0.11.4，1,194 个安装文件与发布清单一致；旧来源备份为 `<维护者本地目录>`。官方 `skills/list` 强制重扫发现四项已启用的 0.11.4 插件 Skill，未返回旧版本路径。
- 工作区：索引与图谱已同步，覆盖 7 个 Project、13 项 Skill、6 个 Agent，审计器 6 项测试通过。核心审计仍为 4/5：既有 `.agents/skills -> ../skills` 被判为第二来源，已再次核实为同一目录；本次未调整发现入口或审计策略。
- 验证边界：三类任务的前后对照属于同一任务的规则走查，未做独立 Agent 盲测，不以字数或格式通过证明 AI 执行质量。新版本发现已核实，未新建用户任务验证整条生成流程，未修改 Hook 信任，也未新增 Windows 或最低运行版本支持声明。发布包位于 `output/releases/0.11.4/`，未公开上传。

## 0.11.3 验证记录（2026-09-03）

- 本地方法调整：默认先完善 PRD，再按当前明确范围细化 Spec。架构从 PRD 讨论中按实际影响反馈可行性、责任与依赖边界；生成或修订 Spec 时共同形成页面行为、必要工程决定与验收要求。已授权实现前本批 Spec 须足以执行，已有充分工程约束直接复用；仅请求 PRD 不扩展为页面开发，映射与阅读不触发新设计。
- 修订范围：共享产品文档、交互 Spec、架构 Skill 及必要参考，工具 README 和原 PRD 的对应入口，以及工作区图谱。页面行为与工程约束在图谱中合并为同一 Spec 细化环节；工作台运行实现、人工映射、正文写回及 Hook 协议保持原实现。
- 验证：三项修改后的 Skill 结构检查、正式 PRD 格式校验及差异检查通过。项目统一 `npm run verify` 通过，含 130 项 Node 测试和 6 条真实浏览器回归。发布归档通过校验、移动到中文空格目录后解压、隔离预检、安装、重复安装、安装副本启动、资源读取、目标文件不变、MCP 与 SessionStart 检查；本轮验证截图已另存并恢复原有输出文件。
- 实际安装与发现：通过 Codex 官方命令将现有 `interactive-product-spec@personal` 更新至 0.11.3，1,190 个安装文件与发布清单一致。旧来源保留为 `<维护者本地目录>`。官方 `skills/list` 强制重扫返回四项已启用的新版插件 Skill，与共享入口路径均有效，未返回 0.9.3 旧路径；未手工修改安装缓存、Hook 信任或历史会话。
- 工作区：Skill 索引与图谱已同步，覆盖 7 个 Project、13 项共享 Skill、6 个 Agent，审计器 6 项测试通过。核心审计仍为 4/5：既有 `.agents/skills -> ../skills` 被判为第二来源，已核实仍指向同一共享目录，本次未改发现规则或审计策略。
- 验证边界：按仅修订 PRD、无 HTML 时细化 Spec、已授权实现、局部修订与映射等场景复核了规则的顺序和范围；这属于规则复核，未新建 Agent 任务演练整条生成流程。发布包保存在 `output/releases/0.11.3/`，未公开上传；未新增 Windows、最低运行版本或新用户 Hook 信任的支持声明。

## 0.11.2 验证记录（2026-09-03）

- 变更范围：统一 Spec 标准明确 HTML 的交付文件、打开方式、资源与网络依赖，以及源码复核、重新构建和最终文件的浏览器检查。持续产品由当前 Agent 维护源码并构建；共享 `web-prototype` 已收窄到一次性草图。插件打包同步已清理的文档与架构 Skill，旧独立实现合同模板和校验器不再进入安装包。
- 完成协议：SessionStart 引用共享 Spec 标准的适用交付检查，再接续原有上报和同轮文档同步；上报接口仍只检查路径与版本，不承担页面质量判定。工作台界面、映射与正文写回实现未改动。
- 验证：项目统一 `npm run verify` 通过，包含架构、契约、UI 构建、130 项 Node 测试、6 条真实浏览器回归流程和差异检查。插件包通过移动目录后的预检、隔离安装、重复安装、安装副本启动、资源读取、目标文件不变、MCP 与 SessionStart 检查。最初的浏览器启动被沙箱阻止，获得运行权限后完整验证通过。
- 实际安装：已通过 Codex 官方命令升级现有 `interactive-product-spec@personal` 至 0.11.2；1,190 个安装文件与发布清单一致，随包共享 Skill 文件已逐一核对。安装来源保持原位置，旧来源备份为 `<维护者本地目录>`，未手工修改安装缓存或 Hook 信任。
- 工作区：Skill 索引与图谱已同步，覆盖 7 个 Project、13 项共享 Skill、6 个 Agent，生成后无待更新来源；审计器 6 项测试通过。核心审计仍为 4/5，既有 `.agents/skills -> ../skills` 入口提示未变，已复核其解析到同一共享源。
- 发布与接入边界：发布包在 `output/releases/0.11.2/`，未公开上传。上述检查证明方法包、工作台和安装副本的对应范围，不代表任意产品 HTML 已按 Spec 验收。当前长任务未验证新任务的 Skill、MCP 与 Hook 加载；Windows、最低运行版本和真实新用户接入未新增支持声明。

## 0.11.1 验证记录（2026-09-03）

- 页面交付流程：已取消 PRD / Spec / 暂不处理及 PRD 类型选择，改为同一任务核对并同步两份文档。旧选择状态与 covered 声明不能免除同步；组件、JS、CSS 改变页面而 HTML 入口未变也适用。
- 代码验证：130 项 Node 测试通过，其中 19 项完成协议定向检查覆盖连续十轮、同 revision 重试、入口未变的组件修订、旧决定兼容、无弹窗 MCP 和路径边界；架构、契约、UI 构建及差异检查通过。现有 6 条真实 Chromium 工作台旅程全部通过，页面导航、映射、局部写回与只读交付保持原路径；最后补充触发条件未改变 UI 实现。
- 文档与共享方法：两项文档 Skill 结构检查、插件 PRD 格式解析及正文差异检查通过；原统一 Spec 的工程章节、稳定 ID、人工 Map 与候选边界保留。图谱是共享治理视图，不另建产品 PRD/Spec；本次产品行为已写回插件原 PRD 和现有架构设计。
- 实际安装：最终包经过移动目录、预检、隔离安装、重复安装、已安装工作台启动、目标 Project 文件不变、MCP 清单与 SessionStart 输出核验。已通过 Codex 官方命令将本机插件更新为 0.11.1；1,195 个安装文件与发布清单一致，28 项 Skill 文件与共享源一致。实际安装服务器上报图谱返回两份文档待同步任务，客户端对话次数为 0；这不证明 Agent 已完成任意产品的语义同步。
- 工作区：图谱 7 个 Project、13 项共享 Skill、6 个 Agent 的覆盖及来源检查通过，生成后无待更新来源。核心审计 4/5：既有 `.agents/skills -> ../skills` 仍被审计器报为第二来源，已核实解析到同一共享目录；本次不改发现入口或审计策略。审计器自身 6 项测试通过。
- 发布包：`output/releases/0.11.1/` 包含不可变归档、校验清单与冻结 Skill 来源；未公开上传。旧安装源保留为 `<维护者本地目录>`，过渡版本备份为 `<维护者本地目录>`，均未改为安装缓存。
- 接入边界：未修改 Hook 信任；当前长任务未加载新 MCP 工具，因此安装副本通过实际 stdio MCP 调用核验。正常新任务的 Skill / Hook / MCP 加载仍待下次启动确认。Windows 实机、Node 20.11 最低版本实机、真实新用户接入和公开发布仍未新增支持声明。

### 三轮反向复核

1. **目标与范围**：覆盖用户从页面反馈形成需求的过程；PRD 与 Spec 都核对但只改差异，探索内容不升级确认，冻结版本和无关文件不被改写。同步所需格式补齐由原文档 Skill 承接，不再让用户另选流程。
2. **完成事实与恢复**：工具响应只表示待同步，相同 revision、旧选择、covered 与中断重试都不能被当成已完成；冲突或失败必须留下明确未完成项。正式结果回到当前文件，不建立新的同步状态库。
3. **工程与兼容**：补齐组件 / JS / CSS 修订但 HTML 入口未变的触发边界；无表单能力也能工作，不启动映射或压缩，不影响原工作台写回与人工关系。安装版本、运行协议和共享方法一致；新任务接入与语义验收不由机械测试代替。

import { RuntimeManager } from "./components/runtime/runtime-manager"
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
    <RuntimeManager />
  </StrictMode>,
)

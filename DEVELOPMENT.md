# 咔哒 · 产品工作台

一块块搭起，一步步完成。咔哒是一个本地优先的可视化产品工作台，用于将完整、精简的 Product Spec 按 Action / Surface 关联到实际页面，让研发从页面及其说明理解全部适用需求，无需另读 PRD；相关 PRD 仍可独立打开。工程与插件标识沿用 `interactive-product-spec`。

当前工具版本：`0.12.7`。工具版本与所加载产品的 PRD/Spec 版本、数据格式版本分别管理。

## 随包的产品工作能力

七项 Skill 及所需资源均在仓库 `plugin-skills/` 中，发布包直接内置。客户无需取得维护者的共享目录或另装个人 Skill。共享方法由维护者显式同步到发行快照，随新版本升级，详见 [RELEASE.md](RELEASE.md)。

用户安装本插件后，Agent 可使用七项 Skill；目标 Project 无需预装个人 Skill。PRD 与 Spec 编写、既有文档转换及工作台接入统一由 `product-documentation` 提供；软件架构与保真压缩能力继续保留。其余随包能力：

| Skill | 用途 |
|---|---|
| `grilling` | 逐项审问和论述想法，提供推荐答案，产品决定由用户作出 |
| `diagram-design` | 将想法、流程和关系表达为 PRD 中的图或独立图形，随包保留模板和校验脚本 |
| `ui-ux-pro-max` | 视觉方向、配色、字体、组件状态与界面检查，随包保留查询脚本和数据 |
| `ant-design-html` | 依据 PRD／Spec 生成 PC 或 APP 形态 HTML；新工程提供 Ant Design／Ant Design Mobile 起步代码，已有工程沿原组件体系 |

用户可以直接说“帮我把想法论述清楚，按需要画图，再做成手机 APP 形态的 HTML”。当前 Agent 按任务选择 Skill 并接续已有文档流程；工作台不新增生成页面或独立模式。APP 在此指手机应用的页面和交互，最终交付 HTML，原生能力按真实实现或仿真说明。

新起步工程首次安装依赖需要 npm 下载或已有缓存，构建后的默认 HTML 内嵌 CSS／JS，可双击打开。UI UX Pro Max 的可选查询脚本需要 Python；不可用时仍可读取随包数据继续设计。上述资料与模板是能力基础，实际用户需求仍须由 Agent 实现和验证。

## Spec 来源派生与手改同步

已明确来源绑定的文档可以由程序生成研发 Spec：PRD 保存产品规则，独立架构保存技术设计。程序提取完整来源，Agent 只在设计来源或承接手改时处理语义。

```sh
node cli.mjs derive-spec --project <Project> --prd <PRD.md> --architecture <Architecture.md> --output <Spec.md>
node cli.mjs spec-changes --project <Project> --spec <Spec.md>
```

Spec 仍可在原工作台或外部编辑器保存。有手改时，先把差异回流相应来源，运行 `derive-spec --preview` 核对候选，再用逐项回执发布；未经承接的手改不会被直接覆盖。正常派生不调用模型，不修改 Map 或产品确认状态。来源声明、回执与状态目录用法见随包 `product-documentation/references/spec-derivation.md`，实现边界见[派生设计](architecture/designs/spec-derivation.md)。现有非派生 Spec 沿原编辑流程，首次迁移需明确绑定和当前入口。

## 产品与技术文档入口

- [`product-blueprint.md`](product-blueprint.md)：跨能力产品边界、端到端主链、核心对象、状态所有权和探索方向。
- [`drafts-documents/Interactive Product Spec产品需求文档.md`](drafts-documents/Interactive%20Product%20Spec产品需求文档.md)：面向研发连续阅读的产品需求文档草稿；用户未确认前不进入 Confirm。
- [`drafts-documents/Interactive Product Spec工作台详情状态交互.spec.md`](drafts-documents/Interactive%20Product%20Spec工作台详情状态交互.spec.md)：本批 Spec 节点详情状态分流，以及评审模式连续只读浏览交互草稿。
- [`architecture/designs/product-work-lifecycle.md`](architecture/designs/product-work-lifecycle.md)：从 Spec 工具演化为产品工作闭环后的目标技术架构与迁移设计，当前状态为 proposed。
- [`ARCHITECTURE.md`](ARCHITECTURE.md)：已经实现并由当前验证入口约束的代码职责。

本文只维护安装、启动、使用方式和当前能力概览。跨能力产品事实、详细产品行为、目标技术设计和已实现代码架构分别回到上述拥有它们的文档；如有冲突，先修订对应正式入口，不在 README 中静默裁决。

它不会根据标记位置生成产品规则。它保留五个职责层：

- `{产品名称}产品需求文档.md`：需求来源与范围解释，可独立于 Spec 进入工作台，但不再是理解 Spec 的必读前置。
- `prd-map.json`：页面/路由与中文 PRD 章节 ID 的关系；与 PRD 位于同一产品文档目录并一起冻结，不复制正文。
- `{product}.spec.md`：统一保存页面行为、公共约束、工程约束和验收的正式正文；普通工作台可逐业务节点直接写回，Codex gate 也可在版本校验后修订，工程章节始终保留在原文。
- HTML：可操作的页面实现。
- `spec-map.json`：Spec 节点与 DOM 元素之间经人确认的关联。

`product.spec.json` 只允许作为工具侧可重建的内部投影或兼容测试夹具，不是项目内需要选择、维护、冻结或交付的正式产物。PRD 正文仍由产品文档流程维护；工作台只写 `prd-map.json`，不会借页面绑定改写 PRD。

## 快速开始

### 普通用户：让 Codex 代为安装

把 [INSTALL.md](INSTALL.md) 或其链接交给 Codex，并说“请按这份说明帮我安装并验证”。Codex 负责检查环境、取得预构建包、调用官方插件安装命令；用户只确认必要权限和启动脚本信任，再在新任务中打开目标项目。插件安装一次，多个 Project 共用，不需要 DMG/EXE 或按项目复制。

公开仓库为 [anbozzz/kada-product-workbench](https://github.com/anbozzz/kada-product-workbench)，安装包与 SHA256SUMS 在同一版本 [Release](https://github.com/anbozzz/kada-product-workbench/releases) 提供。不要把源码 ZIP 当成安装包，也不要假定 npm 已发布本工具。发布者见 [RELEASE.md](RELEASE.md)。

### 源码开发与已有安装的使用

安装包运行需要 Node.js 20.11 或更高版本；源码构建还需满足 UI 构建工具的引擎要求。

插件先形成可独立评审的完整 PRD，经本批工作台评审后，维护适用的独立架构设计，再按明确来源程序派生统一 Spec，再进入已授权实现。复杂功能写明正常与异常结果、状态变化及恢复；简单功能按实际需要展开。架构在 PRD 讨论中反馈可行性和责任边界，独立保存必要工程机制。用户明确要求联合草稿、直接生成或暂缓评审时遵从，并保留实际确认状态；不要求无关范围冻结。交付 HTML 前按 Spec 检查最终文件，之后同轮核对两份文档。仅请求文档时不开发页面，既有 Project 沿原入口维护。

在 Codex 中直接调用 `$product-documentation` 的 Spec 编写路径时，同一任务读取并校验 PRD 后即可先定义页面交互，不以已有 HTML 为前提；有实际页面时再双向核对需求与全部操作。需要工作台时，从同一 Markdown 派生页面与节点视图，不把工程章节变成业务节点或另建页面 Spec。仅拟定路由不能充当实际页面证据。普通操作不因 PRD 已说明而省略，共用规则直接关联适用 Action / Surface；用户请求映射且有已确认 HTML 时继续既有映射流程，不要求用户到项目中心创建第二条投影任务；每轮文档同步不自动启动映射交互。

历史补充型 Spec 仍可按原内容映射和评审，仅在明确生成/刷新时增补，保留有效 ID 与人工映射。投影服务在生成及缓存复用时核对标准 Markdown 节点 ID 与类型，拒绝遗漏或凭空新增节点；这只证明节点留存，不代替 PRD 产品语义覆盖检查和人工确认。

没有 Spec 时，合法 PRD 仍可独立打开页面工作台、建立页面到 PRD 章节的关系并阅读全文。PRD 格式不符合合同时失败关闭：原文件保持不变，界面给出具体结构错误，并允许用户明确创建一条可见 Codex 对话，由 `$product-documentation` 在 `drafts-documents/` 生成标准化草稿；草稿重新通过格式合同后才切换来源并继续绑定。PRD 草稿与 JSON 投影共用同一用户任务创建入口，任务卡在左侧刷新延迟时也提供“在 Codex 中打开”，不再只显示无法操作的 thread ID。

普通工作台每 3 秒检查当前已选 PRD／Spec 文件；有变化时显示“加载新版”，PRD 抽屉打开时提示在抽屉内。点击后保留画布与阅读位置，编辑和未保存关系完成或取消后才能加载。格式错误保留旧版并可重试；标准 Markdown 可直接更新视图，旧自由格式需从项目来源重新生成。PRD 批注发布和冻结评审包沿各自原有机制。

## HTML 完成后的文档同步

安装插件后，Codex 在会话启动时获得完成协议。以下同步仅适用于明确的产品文档流程；纯源码抽取、隔离预览及未纳入该流程的功能试做不自动上报或创建文档，详见 [协议适用范围](architecture/designs/html-delivery-document-routing.md)。当前任务创建或实质修订用户要求交付的本地 HTML 页面，按 Spec 标准完成本批交付文件的适用检查后上报一次，再在同一任务核对 PRD 和统一 Spec、保存必要修订后交付。通过组件、JS 或 CSS 改变页面时，即使 HTML 入口文件未变也上报该入口。无需选择文档或 PRD 类型；连续修订每轮核对，不积压到最后回填。

`product-documentation` 先核对 PRD 的需求与规则，再核对 Spec 的页面、路由、操作、状态、工程约束和验收。每轮从当前文件与明确反馈开始，只修订受影响内容；已有覆盖说明无需修改，缺少产品文档时生成适用范围的草稿，非产品 HTML 说明不适用。探索方案、仿真和未决冲突保留原确定程度，保留有效 ID、工程章节与人工 Map。同步不自动打开映射工作台；PRD 内容修订按产品文档流程整理表达并复核；不自动追加压缩或工作台映射。

`publish_html_ready` 只校验指定 HTML 路径和 revision，返回 `document-sync-required`，不读写产品文档、不证明文档已同步。同一批次重试仍要求核对；旧选择记录和 `prdFollowup` 不再跳过任一文档。完成结果需说明两份文档各自的实际修订或无需修改依据；遇到冲突、写入失败或用户明确暂缓，说明未完成项，不能虚报同步完成。

插件不使用 Stop、UserPromptSubmit 或目录扫描猜测交付。只读 HTML、聊天代码、图片、仅 PRD 修订，以及构建测试偶然产物不上报。PRD 先行且用户已授权页面时，当前 Agent 先完善适用 Spec 和必要工程决定，再连续完成页面及上述同步；只要求文档时不擅自开发页面。原工作台的来源确认、人工映射、评审和写回路径保持各自边界。

### PRD 批注与多轮预览

PRD 首次生成并校验后，用户未安排下一步时，原任务调用 `request_prd_review`，在 Codex 中询问“是否现在开始评审”，提供“开始评审 / 稍后评审”。明确开始后才打开工作台；稍后或关闭不打开，原任务保留 PRD 链接，并提示回复“开始评审”即可继续。同一版本重复调用或工具重连不重复询问。已有明确评审授权或稍后回来时直接调用 `open_prd_review`；明确暂缓或直接生成 Spec 则遵从，不把授权当作确认。客户端无表单能力时回到原任务询问，不默认开始。

两项工具传入 Project、PRD 的绝对路径，已有 HTML 时另传 htmlPath。打开后仍是同一个工作台：有 HTML 时保留画布与原可拖宽 PRD 抽屉，尺寸、缩放、适应画布、拖拽及页面操作均保留；无 HTML 时全宽打开同一个阅读器，关闭后从“打开 PRD”继续。不要求已有 Spec 或页面关系，不自动创建 Map。已有批注会话中的修订发布不重复询问。

批注抽屉顶部为 48px 紧凑工具栏，仅保留状态、正文/差异切换、更多和关闭；完整标题与说明不重复占用正文空间。右上角 × 关闭后可从原 PRD 入口重新展开，草稿仍保留。“更多 → 重新读取本地 PRD”用于载入文件中尚未发布到工作台的改动，不发送意见、不回滚文件；修订进行中禁用。该恢复动作不再常驻底部。

选中文字后，选区旁立即出现评价浮窗；直接用文字填写修改建议，不需要选择评论、替换或删除类型。需要删除时写“删除这段”，需要替换时写明新内容。点击“添加”或 Enter 加入下方待发列表，再继续选择下一处。换选区时已写评价自动保留为待发项；Esc、点击别处或收起抽屉只收起浮窗，未完成评价可继续填写，同地址刷新可恢复。浮窗里的“添加”不发送、不改写正文；旧批注的类型仅作兼容保留，不提供新的类型选择入口。

底部采用 [Beautiful UI Prompt Bar](https://www.beautifului.dev/#prompt-bar) 的 MIT 源码适配版，来源和调整记录在 `ui/src/components/beautiful-ui/`。它汇总原文与各自评价，支持预览、编辑、移除，并可补充整批或全文说明。眼睛按钮预览全部待发内容，发送按钮或底部 ⌘ / Ctrl + Enter 将整批加入队列；底部 Enter 换行，选区浮窗 Enter 仅添加评价。有未完成选区评价时先完成或取消，不要求先保存。

发送后立即可以收集下一批。队列区分“本机等待”“原任务已领取”“正在请求停止”“已停止”和历史；等待中的批次可撤回，进行中的只能请求停止，确认停止前不执行下一批。撤回内容保留在历史；已有新草稿时不会覆盖，可稍后取回编辑。停止不回滚已经写入的内容。

原任务直接调用 `wait_prd_feedback`，省略 timeoutMs，由插件内部挂起等待状态事件（最多 12 小时），原子领取最早批次，使用 `$product-documentation` 修订同一 PRD；步骤之间及写入前调用 `get_prd_feedback_status`，发现停止请求就停止后续修订，在确认没有仍运行的写入后调用 `acknowledge_prd_stop`。无反馈时不向模型返回 pending；显式短探测或最长等待到期返回 pending 后停止本轮等待，用户明确继续才再次调用。客户端超时或工具不可用时也停止并说明，不用终端轮询替代；批注仍保留，可在原任务恢复。

这是合作停止协议，不是桌面原生中断，需立即中断时在 Codex 中停止任务。正常完成后以文件 SHA-256 revision、批次 ID 和修订说明调用 `publish_prd_revision`；返回 `nextFeedback` 时直接处理其中已领取的下一批，没有下一批时再调用默认 `wait_prd_feedback` 挂起。同一地址更新全文并提供上轮差异；等待超时不是结束，不默认修改 HTML/Spec。

发送后的批注和轮次保存在工具状态目录的 `prd-reviews/<sessionId>.json`，不进入 Map 或只读导出。v1 未记录接收的 submitted 批次迁移为待核对，防止重复执行。发送前的选区、输入和待发意见按会话暂存在当前标签页，支持收起、刷新、新版到达后保留；存储不可用时提示不要关闭或刷新。发送失败保留草稿，原任务断线后可带原 `resumeSessionId` 和来源重新打开，但更换地址不保证恢复浏览器草稿。旧版队列等待人工核对；本会话发布新版后，已发送队列继续按顺序处理，保留提交快照并对照当前新版，不按旧行号直接覆盖；未发送草稿的显式适配只接受唯一且未改动的原文上下文，冲突需重新选区。外部改动和旧协议待核对批次仍需核对。

此闭环需使用包含 PRD MCP 工具的插件构建，并由原任务持续参与。模拟消费者验证不等于已升级本机插件或完成桌面原生中断验证；安装与新任务加载仍需单独确认。设计见 [ADR-0007](decisions/ADR-0007-prd-batch-queue-and-stop.md)。插件默认打包仓库内的 Skill 发行快照；维护者通过显式同步更新共享方法，不从安装缓存反向复制。

插件的 `.mcp.json` 为 `html-delivery` 指定 `cwd: "."`，由 Codex 解析为已安装插件根目录；相对入口 `./src/html-delivery-mcp.mjs` 不能继承被评审产品的目录。排查工具未加载时，先用 `codex mcp get html-delivery --json` 核对实际启动目录，并从产品目录验证初始化；仅在工具源码目录手动启动成功不能证明安装连接有效。

```bash
cd apps/interactive-product-spec
npm install --prefix ui
npm run build:ui
npm run validate:example
npm run verify
npm start
```

已有完整工具目录时，在目标项目目录中运行（路径替换为真实已安装入口）：

```bash
node /实际工具目录/cli.mjs .
```

工具会把当前工作目录作为明确的扫描范围，并区分 PRD、Markdown Spec、HTML、`prd-map.json` 与 `spec-map.json`。独立启动适合来源确认、页面关联和只读查看。工作台从页面顶部始终提供“相关 PRD（n）”入口：绑定模式展示完整文档并允许选择章节；评审模式只展示当前页面已绑定章节，未绑定时显示空状态。两种视图都保留目录、搜索、版本/状态。内部 Spec 投影由工具校验并保存在工具侧，不要求研发在项目中理解或维护 JSON。

PRD 正文中的本地图片在原位置显示，拖动右下角可等比例放大、缩小，也可点击“放大查看图片”查看细节；外层抽屉收窄时图片自动适配。支持所选项目内、相对 PRD 路径引用的 PNG、JPEG、GIF、WebP、BMP（含引用式 Markdown）。单张最多 8 MiB、每份文档合计 24 MiB、最多 100 个不同引用；缺失、超限、外部地址或不支持的图片会单独提示。图片快照随只读评审包保留，不修改 PRD 或图片原件；修改原图后重新打开项目来源刷新。

PRD 发生产品语义变化且仍为草稿或评审中时，原 Codex 任务可调用 `open_prd_review` 打开批注会话。用户可连续发送多批意见，原任务逐批修订并通过 `publish_prd_revision` 发布新版；“结束评审”只关闭会话，不确认文档。“确认当前版本”会把当前 revision 返回原任务，由 `$product-documentation` 完成正式 PRD 确认或冻结，再调用 `complete_prd_confirmation` 复核 `已确认` 状态、产品身份和最终 revision。工作台本身不写入或移动 PRD。正式文档状态、评审会话状态和批注批次状态彼此独立。

由 Codex 发起正式修订时使用统一门禁。来源确认、手动替换、映射、Spec 草稿编辑和“提交 Codex”都在同一个地址完成，不需要在项目中心和另一套工作台之间切换：

```bash
node cli.mjs start --project <项目目录> --html <index.html> --source-spec <product.spec.md> --spec <临时product.spec.json> --map <spec-map.json> --gate --json
```

启动时必须使用 CLI 检查后实际返回的地址，不能假定始终是 `4317`。普通同来源、同模式及同初始路由实例可复用；不同来源及 Codex gate 会话保留独立地址，不覆盖其他任务的来源或接收者。端口占用时使用可用端口，并在运行管理中明确列出。

本次 Skill 会话的启动上下文会持续保留到用户确认来源，页面刷新或重复读取启动配置不会让界面退回最近的旧项目。只有确认当前来源，或用户明确点击“返回项目中心”，该启动上下文才会清除。

需要手动管理项目或处理异常来源时，打开终端中输出的项目中心地址并按三步操作：

1. 在项目中心添加项目，选择项目目录、单个 HTML 页面包或本地开发地址；“继续 Spec 绑定”会先实时校验已保存来源，校验成功后直接恢复工作台，失败才进入来源向导。
2. 明确点击“扫描来源并继续”，再处理系统推荐的页面入口、标准化 PRD、可选 Markdown Spec 与两类 Map。合法 PRD 不要求 Spec 存在。
3. 首次建立 PRD 页面关系时，明确确认在 PRD 同目录创建 `prd-map.json`；存在 Spec 时，再处理独立的 `spec-map.json` 写入边界。

扫描结果只是候选。缺失项及其处理入口会在第二步集中显示，不会因为存在其他候选而被隐藏。

项目中心默认把最近项目、内部 Spec 投影和新建的 Spec 映射保存在工具侧。`prd-map.json` 是明确例外：它保存在 PRD 同目录，作为与产品文档一起冻结的页面索引。格式不合格的 PRD 可以保留为来源材料，但不能创建 `prd-map.json`；工作台不会自动改写它。只有用户点击“创建 Codex 对话并生成”后，产品才用独立的受限 Project 写任务在 `drafts-documents/` 生成草稿，原 PRD 和其他制品不在写入范围。

最近项目最多保留 20 个。项目中心可通过项目卡片上的移除按钮释放名额，并在二次确认中显示影响范围：工具侧创建的 Spec 映射会随项目记录删除；PRD、`prd-map.json`、HTML、Markdown Spec 和内部投影始终不在删除范围内，当前正在使用的项目必须先离开工作台。

内部 Spec 投影与旧映射不兼容时，项目中心把旧 `spec-map.json` 标记为失效证据并原样保留，不会继续套用、覆盖或自动迁移。用户明确确认后，工具按新 `productSpecId` 创建独立的工具侧映射；旧版工具侧映射路径仍可由最近项目恢复。

工作台顶部保留“绑定模式 / 评审模式”显式入口。进入可编辑绑定模式后，完整的 Spec 绑定工作台常驻：左侧是稳定的“页面 → 子页面 → 节点”任务导航，中间是可操作页面，右侧是定义、关系和页面映射详情；拖拽、自动候选、手动选择位置、重新关联与解除映射均在此模式完成。左侧页面或节点点击可单向驱动中间导航与定位；中间页面内的浏览不会反向改变左侧展开、滚动或当前选中节点。模块不占据树层级，只作为筛选、节点标签、搜索字段和右侧公共约束归属。评审模式采用页面优先布局：功能页面默认占满工作区，顶部“查找 Spec”（提示为“查找并阅读 Spec”）或 `⌘K` 打开按需列表；桌面端选择节点后列表保持打开，右侧只读详情随选择更新，窄屏则进入详情并可返回原搜索与展开位置。评审详情只包含操作定义和关联约束，不展示页面映射、映射任务筛选、编辑入口或候选发现；点击页面 Spec 标记会收起查找列表并直接打开操作定义。两种模式都会识别当前页面并定位已有映射节点。

映射模式中，左侧只保留稳定的“页面 → Spec 节点”列表，不再展示当前页候选摘要或批量确认入口。用户从列表选择 Spec 后，在右侧“页面映射”中确认、重选或解除页面位置。选择 Spec 本身仍用于定位和查看：页面存在明确地址时先进入所属页，再在页内寻找位置；query 与 hash 一起解析，不保留入口的旧参数。页面能否打开不依赖子组件绑定或候选分数。未找到组件时保留已打开页面，绑定模式可手动选择位置，评审模式只读预览可靠位置。没有明确地址时只辅助当前页，不遍历业务控件检索未知路径。定位期间页面以浅色虚线提示允许绑定的语义目标；成功后自动恢复页面操作并回到“操作定义”，`Esc` 可取消。建立、调整或解除映射后，`spec-map.json` 会自动保存。点击画布空白或页面中的非输入区域会释放侧栏控件焦点，使空格立即用于临时拖动画布；页面内部输入框、文本域、下拉选择和可编辑区域仍保留正常输入。普通可编辑项目中，“操作定义”保存会直接原子写入已绑定的本地 Markdown Spec 原文件，并同步工具侧内部投影，不依赖 Codex 连接。只有工作台本身由 Codex 使用 `--gate` 启动时，节点修改才暂存为会话草稿，点击“提交 Codex”后返回给等待中的原任务。

左侧使用稳定的“页面 → 子页面 → 节点”导航层级表达从属关系，但不显示或跟随“当前页”；模块不占据树层级，只作为筛选、节点标签、搜索字段和右侧公共约束归属。页面行只显示页面名称、ID 和总进度；展开页面行时先保存等待中的 Map，再按明确的 hash/query 路由单向导航，收起只改变左侧。用户选中一个 `SURFACE` / `ACTION` 后，可以在中间自由浏览到正确位置，左侧仍保留该节点；随后使用右侧“在页面上选择位置”或“重新关联”并点击 DOM 即可纠正自动定位。Map 的人工目标保存确认时的页面地址、selector 和 fingerprint，不增加 `pageId`，不保存或回放用户点击过程，也不借映射操作改写 JSON 页面归组。用户确认的组件关联与自动跳转能力分离：页面地址只用于尽力导航，不要求低保真 HTML 能在刷新后重建患者、会诊或表单状态。目标暂未出现在当前 DOM、但保存地址仍可自动打开时保持显示“已关联”；只有自动导航不可用或已经到达保存地址、仍需用户操作进入动态场景时，才显示“已关联 · 需手动进入”，且不进入“需复核”。用户进入演示场景后，工作台自动恢复定位和高亮，无需重新绑定。selector 多命中、目标类型不兼容或语义指纹变化才进入复核。搜索、状态筛选或用户主动选择可以跨页面展开。常驻筛选为“全部 / 待映射”，只有存在 `invalid / ambiguous / drifted` 时才显示“需复核（数量）”；它不表示 PM 尚未阅读，也不引入已审查、通过/不通过或逐项打勾状态。

操作定义中的正文内容块可双击原位编辑并明确保存当前一条；“页面匹配提示”属于候选计算元数据，不进入操作定义阅读视图，只在顶部“编辑完整节点”中维护。完整节点编辑仍用于标题、状态、来源、匹配提示和内容块结构的整体调整。两种方式共用同一节点级 revision 校验与写回链；单条编辑遇到状态、来源或概括性定义时会同步其既有结构化镜像，不创建第二份数据，也不会在失焦时自动保存。

画布上的映射圆点和定位提示只跟随当前可见目标：被子页面/弹层覆盖、祖先隐藏或裁剪、完全移出画面的目标不绘制标记，也不保留隐形点击区域。返回或滚动使目标再次可见时自动恢复；未被遮挡的公共区域仍保留标记。这只影响显示，不删除或重建已确认的绑定。

展开页面清单时优先打开页面明确地址，不受子组件异常绑定阻断。点击已绑定 Spec 时仍优先恢复人工目标，保存地址需通过唯一目标、类型及指纹校验；恢复失败时可打开所属页明确地址，但不绘制猜测命中、不覆盖绑定。目标文字变为空也视为不匹配；浏览、定位与模式切换均不自动改写已保存地址，只有明确重新关联才更新绑定。详见 [ADR-0009](decisions/ADR-0009-page-first-spec-navigation.md)。

左侧树与映射完成率只包含 `SURFACE` / `ACTION`。选中节点后，右侧“关联约束”按类型展示直接关联的规则、状态、权限、事件、验收和外部依赖，并把同模块但没有直接 relation 的其他非页面节点单独分组；该分组只保证可见性，不推断其适用于当前功能。约束卡片可在当前 Tab 原位展开完整定义，始终标记为“无需页面映射”，不会进入拖拽或页面定位状态。

所有启动方式都保留相关 PRD、Spec 查找/绑定情况、导出、离开与保存状态；门禁会话继续在会话操作组提供“提交 Codex”。点击“离开”后先显示当前 Spec 绑定与保存状态，不会直接清空上下文；用户可以继续当前绑定，也可以明确选择“切换项目或文件”。切换时会先保存等待中的页面映射，然后返回项目中心；项目中心的“继续 Spec 绑定”会实时校验已保存配置并直接恢复工作台，不要求重复扫描。若 Codex 门禁会话中存在尚未提交的 Spec 修订草稿，离开前会明确提醒草稿将被放弃。当前产品是本地工具，不包含虚构的账号登录页。

顶部操作按职责分组：工作模式紧邻产品标题；右侧“评审资料”保留相关 PRD、完整 Spec、Spec 查找/绑定情况。完整 Spec 只读当前已载入 Markdown，可按章节阅读节点外的公共说明与工程定义；加载新版和直接保存成功后同步，未写回的 gate 草稿另作提示。“交付与会话”提供 ZIP 导出、提交 Codex、离开与保存状态。导出默认生成可移植 ZIP 只读评审包，而不是把大型页面强行内嵌为单 HTML。包内冻结当前 HTML 页面包、工作台静态资源、PRD、Spec 与两类 Map，并附带 Windows 10/11 x64 免安装 EXE 和 Node 20 兼容查看器；两者都只监听 `127.0.0.1` 随机端口。页面滚动、点击、缩放、Spec 标记/查找和 PRD 阅读保留，项目扫描、文件浏览、Spec/Map 写入与 Codex 接口全部关闭。

ZIP 只包含已选 HTML 入口同目录下的运行时资源，不包含 `.git`、`node_modules`、源码、缓存、source map 或同目录正式 Spec/Map 文件。使用本地开发地址时不会猜测构建产物；请先构建或选择可独立读取的 HTML 页面包再导出。接收者完整解压后，Windows 10/11 x64 双击 `打开评审.exe`，无需安装 Node；macOS 双击 `打开评审.command`，或安装 Node 20 后执行 `node open-review.mjs`。当前 EXE 未附带企业代码签名，正式跨组织分发前建议由发布方使用可信证书签名。页面依赖的真实后端、登录态、WebSocket 与外部接口不会被离线包伪造。

## 交互与组件栈

- React 19 + TypeScript 6 + Vite 8 作为 UI 工程基线。
- shadcn/ui Rhea + Radix 提供 Button、Tabs、Sheet、Accordion、Select、Tooltip、ScrollArea 等可访问组件。
- 可编辑映射模式常驻 Spec 树、页面画布和映射详情；只读评审模式默认展开可拖动、收起和关闭的 Spec 总览浮层，标题展开与独立定位图标分离，PRD 当前页范围开关默认开启；桌面端 Spec 查找与只读详情可并列连续浏览，窄屏在列表与详情间往返。
- `@dnd-kit/react` 提供鼠标、触控与键盘拖拽；工作台把落点转换为 DOM 元素后只保存选择器与语义指纹。
- `react-zoom-pan-pinch` 提供缩放、平移与自动尺寸检测，并保留桌面/平板/手机手动预设；普通滚轮和触控板二维滑动用于平移，Ctrl/⌘ + 滚轮（包括 iframe 内的触控板捏合）会按连续手势速度加速，再通过逐帧缓动收敛到目标倍率，同时保持手势焦点不漂移；空格 + 鼠标拖动也可平移。首次进入会显示可关闭的快捷操作提示，用户关闭后只在当前浏览器保存这项视图偏好；整条提示底栏随之移除，操作按钮改为右下角悬浮，不再占用画布高度。固定宽度的移动端 HTML 优先按其页面外壳尺寸展示，而非套入固定桌面画布。
- 视觉采用暗黑开发者工具风格的命令中心与分步向导；基础控件继续复用 shadcn/ui 与 Radix，不自行重写交互基础设施。

生产静态资源由 `ui/` 构建到唯一的 `web/` 目录，CLI 只提供这份构建产物，不保留平行的手写界面。

## 命令

```bash
# 当前目录零配置启动；唯一有效来源直接进入工作台
node cli.mjs [项目路径] [--html <index.html>] [--url <本地开发地址>]

# 可视化项目中心
node cli.mjs start [--port 4317] [--host 127.0.0.1] [--state <工具侧配置路径>]

# 兼容已有内部投影的底层可编辑映射
node cli.mjs map --html <index.html> --spec <product.spec.json> --map <spec-map.json> [--route '#screen'] --port 4317

# Codex 统一阻塞式图形修订（--spec 是工具侧内部投影；stdout 最终只返回 review-submission JSON）
node cli.mjs start --project <项目目录> --html <index.html> --source-spec <product.spec.md> --spec <临时product.spec.json> --map <spec-map.json> --gate --json

# 兼容已有内部投影的只读查看
node cli.mjs review --html <index.html> --spec <product.spec.json> --map <spec-map.json> [--route '#screen'] --port 4317

# 校验数据契约
node cli.mjs validate --spec <product.spec.json> --map <spec-map.json>

# 统一验证：架构边界、契约、构建、Node 测试和真实浏览器旅程
npm run verify
```

代码职责、架构变化触发和验证分层见 [`ARCHITECTURE.md`](ARCHITECTURE.md)。

## 可复用性边界

- 工作台不知道医疗、MDT、任务看板等任何业务概念。
- 可映射 Spec 的整张卡片都是拖拽热区；页面标注以 13px 圆点呈现，但保留 24px 可聚焦点击目标，不使用扩大的邻近推断，也不依赖标注下方 DOM 推断。
- 页面标注是可聚焦按钮；悬停或键盘聚焦时会放大、显示光环与“查看 Spec”提示，点击、Enter 或空格均可打开对应定义。
- 自动候选只是决策支持；未经人确认不写入映射文件。
- 项目扫描只读取候选 HTML、Markdown 和兼容 JSON 文件；不会产出业务规则、修改 HTML 或自动改写现有 Spec。Markdown 名称明显属于 PRD/产品需求时不会被当成 Spec 候选。格式不合格的 PRD 不会自动触发对话或重写；只有用户明确发起后才生成独立草稿。
- 普通可编辑工作台只允许逐次修改一个已绑定节点，并以来源 revision 防止覆盖外部变更；保存会原子写回本地 Markdown 原文件并同步工具侧内部投影。只有由 Codex 启动的门禁会话才把节点修改保留为会话草稿，提交后返回差异，由等待中的 Codex 任务决定是否应用到原始 Markdown。
- 最近项目只用工具侧保存的不可扩展配置标识直接恢复，并在服务端重新校验页面、Spec、映射和本地开发地址；校验失败才回到来源向导，不接受客户端借恢复入口更换路径。
- 只有 `SURFACE` 和 `ACTION` 节点直接定位页面。`RULE`、`STATE`、`EVENT`、`PERMISSION`、`EXTERNAL`、`AC` 通过节点关系展示。
- 映射保存确认时的页面/路由上下文、CSS selector 与语义指纹，不保存像素坐标或点击轨迹。页面/路由只用于尽力导航；当前场景未显示目标时保留关联，手动进入后自动恢复定位。
- 单个 HTML 入口支持同目录相对资源和入口目录内的根路径资源。本地开发地址通过同源代理加载，第一阶段仅允许 `localhost`、`127.0.0.1`、`::1` 或 `0.0.0.0`；WebSocket/HMR 不是第一阶段的完成范围，页面变化后可手动刷新画布。
- Hash 路由或查询参数页面可用 `--route '#screen'` 或 `--route '?view=screen'` 直接进入；工作台会把建立映射时的路由保存为上下文。
- 操作节点只能落到可识别的交互目标：原生控件、常见 ARIA 控件、键盘可聚焦/可编辑元素，或以 `cursor: pointer` 明确表达交互的组件边界；区域节点只能落到 `main`、`section`、`region` 等有语义的容器。没有交互语义或明确交互提示的普通 `div` 不会生成映射。
- 统一交互层会把缩放、平移后的视觉落点换算为 iframe 内部的 DOM 坐标；不同项目不需要为画布比例另写定位逻辑。
- 页面缩放、Spec 浮层开关与详情固定状态属于视图状态，不进入 Spec 或映射数据；页面改版后依靠 selector 和语义指纹检测目标不兼容、不唯一与内容漂移，不能仅因当前场景没有显示目标就判定关联失效。

## 数据契约

- [`schemas/prd-map.schema.json`](schemas/prd-map.schema.json)
- [`schemas/product-spec-bundle.schema.json`](schemas/product-spec-bundle.schema.json)
- [`schemas/product-spec-projection-output.schema.json`](schemas/product-spec-projection-output.schema.json)：Codex 结构化输出使用的严格适配层，服务端会移除可选空值后再按正式 Bundle 契约校验。
- [`schemas/spec-map.schema.json`](schemas/spec-map.schema.json)
- [`schemas/review-submission.schema.json`](schemas/review-submission.schema.json)

映射集合使用 [W3C Web Annotation Data Model](https://www.w3.org/TR/annotation-model/) 的 `AnnotationCollection` / `Annotation` 结构，并以本项目的页面导航提示、CSS selector 与指纹扩展页面定位语义；页面地址不是关联有效性的前提。

可映射 PRD 使用 `<!-- prd-profile: 可映射PRD-v1 -->` 标记。`详细功能说明`内每个页面或关键区域章节保留正文及紧随标题的稳定中文 ID；功能内按实际内容组织分支，不要求固定的小节名称或数量。全局规则保存 `规则-...` ID。旧四小节文档继续兼容，工作台完整读取原 Markdown。`prd-map.json` 只引用章节 ID 并保存页面/路由上下文；标题、顺序或正文变化不重建 ID，revision 变化提示复核，不自动写回 Spec 或 PRD。

新投影在 bundle 顶层使用全局 `pages`，每页保存稳定 `id`、标题、可选职责、可选 `parentPageId`、路由提示和可见锚点；有明确页面证据的模块节点通过 `pageId` 引用页面，因此同一页面可承载多个模块。没有页面证据时 `pages` 可为空、节点 `pageId` 保持为空，不得为工作台结构编造页面。`parentPageId` 只能来自明确导航事实，不按标题、数组顺序、route 或 DOM 猜测。旧版 module 内 `pages` 继续按平铺根页面兼容读取。Spec、页面和映射严格分层：Spec 正文只说明功能做什么，页面元数据只说明功能在哪里出现，`spec-map.json` 才说明节点对应哪个具体 DOM；页面树不得反向改变节点正文、拆分粒度或产品语义。节点正文继续使用有序 `contentBlocks`：每块只要求稳定 `id`、原文字段标题 `label` 和 Markdown 文本 `content`。工作台不预设“用户目标、前端行为、后端结果、权限、验收条件”等字段；旧版任意键 `fields` 仍可读取，并会在编辑后无损转为有序内容块。可以投影和展示只表示满足工作台协议，不表示该 Spec 已通过内容治理或产品确认。

## 安全与开源

服务默认仅监听 `127.0.0.1`，限制文件目标资源只能从入口 HTML 所在目录读取，并拒绝把本地开发代理重定向到外部主机。只读评审包使用独立、无写接口的本机查看器，不能替代账号权限或公网发布。Codex 投影只接受项目目录内的 Markdown 和 HTML，输出经过 v0.1 契约校验后才原子写入工具侧目录；使用前需要本机 Codex App Server 可正常运行并已完成认证。项目以 MIT License 授权。

## 多项目局域网发布

当前项目在原工具栏“导出与发布”中输入四位数字密码，点击“开启服务并发布”，随后复制链接与密码给同一局域网同事。原 ZIP 下载保留；发布后可更新内容、暂停、改密码或删除。源文件修改不会自动发布。

项目中心的“本机发布服务”统一启停并列出所有项目，各项目链接、密码与快照独立。关闭服务保留项目设置，重新开启只恢复已开启项目；浏览器或作者工作台服务退出不停止独立发布后台，电脑重启后需手动开启。未设置密码失败锁定。

本批已在 macOS 通过本机浏览器与后台生命周期回归；第二台局域网设备及 Windows/Linux 尚未实机验收。macOS/FreeBSD 的单实例锁依赖系统 lockf，Linux 依赖 flock。发布监听本机 IPv4 接口，地址选择用于分享；不会自动配置防火墙或公网访问。

## 打开、检查和关闭工作台

macOS 双击源码目录的 `打开工作台.command`，或执行 `node cli.mjs open`。入口优先打开现有普通实例；没有时在后台启动，并检查服务身份与页面可读性，成功后才打开浏览器。`--no-open` 只核实并返回地址。关闭终端或浏览器不等于关闭这个后台实例。

原项目中心与工作台顶部显示“工作台 · 已就绪 / 等待来源确认 / 已断开 / 已关闭”。点击打开统一实例列表，可打开或关闭选中工作台；有未保存内容、未响应标签页、未提交草稿或活动文档任务时会阻止关闭并说明原因。关闭编辑工作台不影响局域网发布服务。旧插件已启动的未登记实例不冒充纳管，需通过新版入口重新启动。
